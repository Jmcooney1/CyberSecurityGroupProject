#ReadME

BackEnd
To run the backend, run the command
    npx prismate generate
    node server.js

Frontend
To run the frontend
    npx expo start --clear

You will have to access the env file to change the email and tempary password to a personal account to send the fake email

The fake login information is stored in the users.json file (not secure but we not actually putting log in data into the fake canvaas)
